[pytest]
testpaths = harness/tests
addopts = -q -p no:cacheprovider -m "not real"
markers =
    slow: needs a bare origin, clones and hooks
    real: spends money on a real agent; never run by default
