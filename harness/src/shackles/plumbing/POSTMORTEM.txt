Inputs, all in the round folder: HISTORY.md, FINDINGS/, RESULTS/, the two judgment-call files, the OWNER.log slice, and STATE.json (spend versus quote). Output: POSTMORTEM.md. Put TODOs in docs/TODO.md and CLARIFICATIONS in docs/CLARIFICATIONS.md ({{ step.carry_forward }}), each appended under a "## round {{ round.id }}" heading; the next round's planner reads both files verbatim. These edits land after LANDING and are not charged as living tokens; be dense anyway. Earlier rounds' plans are the PLAN.json of the round folders beside {{ round.folder }}; a component older than the first archived round has no plan: say so and trace it to the spec files.
