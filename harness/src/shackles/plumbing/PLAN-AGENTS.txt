Roster, by rung (model, effort, prices per MTok, spawnCost): {{ project.agents }}
Steps this round, with their gates and whether each gate runs: {{ round.steps }}
Rung forced for every step by start --agent (none: choose freely; else name it for every step, the runner runs and prices each at it): {{ round.agent_override }}
Minimum shares, for the steps and gates present this round: {{ step.minimum_shares }}
Write AGENTS-PLAN.json: agents maps every producer step above that is not overridden, CHAT-TO-PLAN excepted, to a rung; shares.work maps those steps and CHAT-TO-PLAN to fractions of the work budget, CHAT-TO-PLAN at its minimum above (that share is already spent, booked at start, and listing it keeps the other budgets honest); shares.gates maps every producer step whose gate runs to a fraction of the gates budget ({} when no gate runs); each non-empty map sums to 1. Optional: gateAgents (GATE -> rung), subAgents (STEP -> simultaneous sub-agents, at most {{ project.maxSimultaneousSubAgentsPerRound }}). The quote is {{ round.budget }}: {{ project.workFraction }} of it is work, {{ project.gatesFraction }} gates.
