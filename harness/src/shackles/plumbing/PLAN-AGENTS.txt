Roster, by rung (model, effort, prices per MTok, spawnCost): {{ project.agents }}
Steps this round, with their gates and whether each gate runs: {{ round.steps }}
Write AGENTS-PLAN.json: agents maps every producer step to a rung; shares.work maps every producer step to a fraction of the work budget; shares.gates maps every producer step whose gate runs to a fraction of the gates budget ({} when no gate runs); each non-empty map sums to 1. Optional: gateAgents (GATE -> rung), subAgents (STEP -> simultaneous sub-agents, at most {{ project.maxSimultaneousSubAgentsPerRound }}). Minimum shares, for the steps and gates present this round: {{ step.minimum_shares }}. The quote is {{ round.budget }}: {{ project.workFraction }} of it is work, {{ project.gatesFraction }} gates.
