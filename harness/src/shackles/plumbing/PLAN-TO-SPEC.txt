Write SPEC.json and SPEC.md, the spec body the owner reads. verify runs from HARNESS. implPaths and testPaths are HARNESS-relative, prefix-disjoint, and outside the round folder, the locked prose and the spec files. refactor items total at most {{ project.maxRefactorOverhead }} of the quote ({{ round.budget }}). Carry the plan's non-goals verbatim in nonGoals. Paths declared by live sibling rounds, avoid them: {{ step.sibling_paths }}.
