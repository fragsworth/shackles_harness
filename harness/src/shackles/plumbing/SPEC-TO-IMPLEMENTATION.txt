The tests under FROZEN_PATHS are frozen: never change them; a wrong test is UPSTREAM to SPEC-TO-TESTS. verify ({{ step.verify }}) must pass within {{ step.verify_timeout }} s. When MERGE_IN_PROGRESS lists files, the worktree holds a merge with conflict markers: resolve exactly those files, keep both intents, run verify, and run no git command at all; the runner stages and commits the merge.
