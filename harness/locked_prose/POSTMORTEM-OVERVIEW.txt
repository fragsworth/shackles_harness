POSTMORTEM: report on what should change.

{{ prose.COMMON-PROJECT }}
{{ prose.COMMON-ROUND }}
{{ prose.COMMON-OVERVIEW }}

Determine what, if anything, stood out as wrong in this round. First, just identify issues, and rank them by importance. Log them.

For each issue, trace the component(s) causing the issue to the plan(s) that originated them, and the owner words that led to the plan(s), and anything else that will help.

Then, for each issue, decide exactly one:
1) The plan(s), the owner words, the project vision, and anything else you find, are all clearly in agreement, and the owner does not need to clarify: propose the sensible resolution and add it to TODOs for next round.
or 2) Choosing how to resolve it is not obvious from the research, and the owner must clarify: add it to CLARIFICATIONS for owner review with the plan(s) and exact owner words that gave rise to the problem. If possible, find any agent judgment call(s) that led us here.

Write a postmortem summary containing the above as the first section of POSTMORTEM.md, under the heading Summary. The runner charges ${{ project.postMortemCostPerSummaryToken }} per token and shows it to the owner. Minimize costs while writing in plain English.

The gate that judges you will have the following prose: {{ plumbing.GATE-PROSE }}

{{ plumbing.PROCESS-INSTRUCTIONS }}
