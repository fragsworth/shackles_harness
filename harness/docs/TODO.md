# TODO: carry-forward items, one section per round, appended by POSTMORTEM and read verbatim by the next round's planner.

## bootstrap
- Measure whether clean artifacts pass the gates as worded (Level 1 probes) before proposing any prose relaxation.
- Driver-mode costs are estimates unless the Agent tool reports tokens; headless mode books exact costs.
- Deferred: a projectRoot key for a target project outside harness/, lease or TTL for dead claims (rounds reports them), exact tokenizers, cross-round path arbitration beyond W1.
