# Judgment calls — part 2 (JC-36 … JC-71)

Scheme and part 1 (JC-01 … JC-35) are in `JUDGMENT-CALLS.md`. Same format: **JC-nn**
(Kind · Weight · D/U) · *Choice* · *Chose* · *Alternatives* · *Why unsure* · *Affects*.
Calls JC-46 onward were made without an inline tag in the spec at first; the
`Affects` field is the cross-reference.

**JC-36** (TOOLING · light · U) · What counts as "a test function". · A function whose
name starts with `test` in a pytest-named file under `testPaths`, found with `ast`. ·
Any function in any file under the test paths; pytest's collection output. · The owner
says "a test function in a file under testPaths"; pytest conventions are outside the
files. · `12 suite`.

**JC-37** (GAP · light · U) · How an archived test function leaves its file. · Cut the
source segment (decorators included), append to `tests-archive/<same path>` with the
original import block; class-level tests become prefixed top-level functions. · Move
whole files; ask the agent to do the move; store archived tests as text. · "Moved by
the runner" at function granularity forces surgery on Python source. · `12
suite.apply`.

**JC-38** (AMBIGUITY · light · D) · Whether `lostValuePerHour` is booked. · Reported as
an informational line, excluded from totals. · Added to the round total. · "Globally
lost budget per hour of time, for context" reads as context only. · `13
ledger.elapsed`.

**JC-39** (GAP · light · D) · The estimate when the driver reports no usage. · Input =
the prompt file's tokens; output = `estOutputFraction` × input; spawn cost always. ·
Refuse to record without usage; a flat spawn cost only. · `estOutputFraction` exists
for exactly this; nothing says how input is estimated. · `13 attempt_cost`.

**JC-40** (TOOLING · light · U) · The hook command line. · `python
"$CLAUDE_PROJECT_DIR/src/hooks/owner_log_hook.py"`, interpreter overridable at
install. · Absolute interpreter path (machine-specific in a committed file). · Claude
Code hook variables and shells per platform are outside the files. · `14
ownerlog.hook_command`, `20`.

**JC-41** (TOOLING · structural · U) · How "effort" reaches the Agent tool. · An
`effort:` frontmatter field in each generated agent definition, plus a read-only
`-gate` twin per rung. · Passing effort per spawn; one definition per rung without
twins. · The owner states the constraint ("takes effort only from one loaded at
session start") but the definition format is Claude Code's. · `14 agentdefs`.

**JC-42** (TOOLING · owner-facing · U) · Where `.claude/` lives and where the owner
starts the session. · At the harness root (`harness/.claude`), session started in
`harness/`. · Repository root; both. · Claude Code's project-directory discovery is
outside the files. · `15 install`, `20`.

**JC-43** (GAP · light · D) · Where INDEX.md's one-line purposes come from. ·
Mechanically from docstrings, headings, first comment lines; regenerated by the
runner at cleanup. · Hand-maintained by agents with a completeness test. ·
AGENTS.md only says "grep INDEX.md for routing". · `15 index`.

**JC-44** (TOOLING · light · U) · How probes run a real agent outside the driver's
chat. · The `claude -p` CLI with model from the rung, JSON output for usage. · The
Anthropic API directly; a manual procedure. · No spec file names a mechanism for
"real agents run as probes". · `17 tests/probes`.

**JC-45** (GAP · light · D) · Which doctor areas the real-file lint test asserts. ·
Config, lint and render only; drift, generated files and hook are left to the drift
test and to `doctor`. · All areas (fails on a fresh clone before install). · A CI or
fresh clone should still pass the lint test. · `19 test_spec_lint`.

**JC-46** (GAP · light · D) · Finding id scope. · Unique per round (`F<n>`; runner
`R<n>`). · Per step. · Round-wide ids keep resolutions unambiguous across retries. ·
`SPEC.md §5`, `08 state`.

**JC-47** (GAP · structural · D) · How runner-only work (LANDING, the booking after
CLEANUP) is triggered. · Inside `next` (landing) and `record CLEANUP` (booking and
sync); the driver never sees a separate command. · Dedicated `land`/`book`
commands. · AGENTS.md describes only `next` and `record` for the driver. · `10
next_action.advance_through`, `10 recording.finish_round`.

**JC-48** (AMBIGUITY · light · D) · How `maxSimultaneousSubAgentsPerRound` is applied.
· `next` returns one action; the limit is validated in the agents plan and stated in
every prompt's LIMITS; the runner cannot count an agent's own sub-agents. · `next`
returning several parallel actions. · The steps are sequential by construction. ·
`06 process_instructions`, `07 validate_agents_plan`.

**JC-49** (AMBIGUITY · light · D) · What of the producer's message a gate sees. ·
Resolutions and dispute notes only; not the summary. · The full final message. ·
"Never the producer's transcript" versus "prior findings with their resolutions". ·
`10 prepare_gate`.

**JC-50** (GAP · light · D) · Whether NEEDS-OWNER/BLOCKED work is committed. · The
accepted (non-stray) changes are committed on the round branch before pausing. ·
Revert everything; leave uncommitted. · Nothing lands before LANDING, so keeping work
is safe and reviewable. · `10 record_producer step 3`.

**JC-51** (AMBIGUITY · owner-facing · D) · Whether `local.yaml` is committed. · Committed
(it holds the accepted baseline, which CI and every clone need). · Gitignored, with
the baseline in a separate committed file. · "local" suggests machine-local; "no user
input" and the drift test suggest shared. · `01`, `04 localstate`.

**JC-52** (GAP · light · D) · The fate of `round/NNNN` branches after a round. · Kept on
the remote as the record; only worktrees are pruned. · Delete on DONE. · The folder
under `archives/` is already the record; keeping the branch costs nothing. · `02 §7`,
`08 prune`.

**JC-53** (GAP · owner-facing · D) · Whether `start` needs an owner quote. · Required
(`--quote`), verified against the log, stored as `origin_quote`. · Optional. ·
"Without a log, start refuses" implies start uses the log; POSTMORTEM traces to
"owner words". · `02 §3`, `08 rounds.start`.

**JC-54** (GAP · light · D) · The judgment-call tool's form. · A `run.py jc` subcommand
run from the worktree's `src/run.py`. · A separate script. · Fewer entry points for
agents; AGENTS.md names only `run.py`. · `03`, `14 judgment_calls`.

**JC-55** (AMBIGUITY · light · D) · When `driverUsdPerStep` is booked. · Once per step,
on its first attempt. · Per attempt; per `next` call. · "Per step" is the key's name;
retries are attempts of the same step. · `13 attempt_cost`.

**JC-56** (GAP · light · D) · How invalid results (unparseable or schema-violating) are
bounded. · They do not consume gate turns; `invalid_in_row` reaching
`maxRoundAttempts` pauses `INVALID_LIMIT`. · Count as gate rejections; unlimited. ·
No owner sentence covers malformed messages. · `02 §6`, `10 handle_invalid`.

**JC-57** (TOOLING · light · U) · Whether to install a SessionStart hook to catch stale
agent definitions. · No; `doctor` reports and `start`/`next` refuse. · A SessionStart
hook printing the warning. · Hook semantics at session start are outside the files;
fewer moving parts. · `14 agentdefs.staleness`.

**JC-58** (GAP · structural · U) · Language, version and dependencies. · Python 3.11+,
`pyyaml`, `pytest`; hand-rolled JSON validation; no build step. · `jsonschema`,
`pydantic`, a packaging step. · Only `run.py` and YAML files are implied by the
owner. · `SPEC.md §5`, `20`.

**JC-59** (GAP · light · D) · What "the suite" is when the runner verifies. · The
whole `tests/` suite of the worktree via `verifyCommand`, probes excluded by pytest
config. · Only the round's tests; only pre-existing tests. · "Until the frozen tests
pass" plus "every round runs it" for suite tests. · `11 verify`, `20`.

**JC-60** (GAP · light · D) · How `{{ agents.SECTION }}` keys are formed. · Level-2
headings, uppercased, non-alphanumerics to `_`, trimmed; `FULL` for the whole file. ·
Any heading level; exact heading text. · COMMON-STEP-END uses
`agents.DEFINED_AND_UNDEFINED_JUDGMENT_CALLS`, which matches a `##` heading only after
normalisation. · `05 prose.section_key`.

**JC-61** (AMBIGUITY · light · D) · The exact meaning of `maxTurnsPerGate: 3`. · The
third FAIL of a producer pauses the round (three rejections allowed, then pause). ·
Three gate attempts including the passing one; three producer attempts. · "Turns
(rejections) per gate" is close to but not exactly a count of attempts. · `10
fail_producer`.

**JC-62** (GAP · structural · D) · Where owner answers to plan questions live. ·
Inside `PLAN.json` questions (`answer`, `quote`), each quote verified at record. · A
separate `record --answer` flag per question; STATE.json only. · "No round starts
with unanswered questions" needs a checkable place. · `07 PLAN.json`, `10
record_plan`.

**JC-63** (GAP · light · D) · How `project.remaining` is computed. · `budget` minus the
sum of every round's ledger total found under `archives/rounds/` plus the active
round. · A running number in `local.yaml`; not offered. · The token exists in
COMMON-PROJECT; the budget is "just for context, can become stale". · `06
context.all_rounds_spend`, `13 project_remaining`.

**JC-64** (GAP · light · D) · `abandon` after a landing. · Ends the round without
POSTMORTEM/CLEANUP but still syncs the round folder to main and books nothing. ·
Refuse abandon after landing; book charges anyway. · "The round ends without
landing" describes the pre-landing case only. · `08 rounds.abandon`.

**JC-65** (GAP · light · D) · Unknown keys in agent-written JSON. · Invalid (the exact
shape is in every prompt). · Ignore silently. · Strictness catches drift between
prompt text and code early but costs retries. · `07`.

**JC-66** (AMBIGUITY · light · D) · Which bytes the plan and spec archive charges
cover. · Whole `PLAN.json`; `SPEC.json` plus `SPEC.md`. · Only the plan's `text`;
only `SPEC.md`. · "For approved plans" and "for specs" name artifacts, not fields. ·
`13 archive_charge`.

**JC-67** (CONFLICT · structural · D) · What "plus the round folder" grants a
producer. · Only its own artifacts and the two judgment-call trails; runner files in
the folder are strays. · The whole folder. · AGENTS.md grants the folder; the same
file says only the runner writes its state; protecting STATE.json seemed the intent.
· `05 resolve_scopes`, `11 checks.classify_changes`.

**JC-68** (GAP · light · D) · Whether implementation may edit tooling files
(`pyproject.toml`, `.gitignore`). · No: declared paths are living paths only; the
owner edits tooling by hand. · Add the harness-root tooling files to the scope. ·
"Only within their step's declared paths" leaves the declaration to the harness. ·
`05 table`.

**JC-69** (GAP · light · D) · Harness-owned sentences in the CHAT-TO-PLAN plumbing
(`chat_prompt_note`). · A fixed mechanical paragraph naming where PLAN.json goes and
the record command. · Nothing beyond the generic plumbing. · The settings-surface rule
discourages harness text, but the driver needs the mechanics. · `06
prompts.chat_prompt_note`.

**JC-70** (GAP · light · D) · The mechanical check for SPEC-TO-TESTS. · Tests must
collect; they are not run (they are expected to fail). · Run and require failures;
no check. · "Before it exists" implies failing tests are normal. · `02 §4`, `11
verify collect mode`.

**JC-71** (AMBIGUITY · light · D) · The base of `hardStopBudgetMultipleIndividual`. ·
The attempt's step share × quote (skipped when 0). · The default share; the whole
quote. · "Applied to a single agent and its own budget" — the agent's budget is the
plan's share. · `10 pauses.check_individual`.
